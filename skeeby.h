/* INCLUDE GUARDS */
#ifndef SKEEBY_H
#define SKEEBY_H


/* With this function we can get string input from a user.
   The function takes in a character array as input, which acts as    the prompt to the user*/
char *get_string(char s[]); 







#endif
